[package]
name = "asahi-migration"
version = "0.1.0"
edition = "2024"

[dependencies]
sea-orm-migration = { version = "=1.1.19", default-features = false, features = ["runtime-tokio-rustls", "sqlx-sqlite", "with-chrono"] }
